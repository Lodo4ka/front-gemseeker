
import { Checkbox } from "shared/ui/checkbox"
// import { $isChecked, toggled } from '../../model';
import { useUnit } from "effector-react";
import { $isBuyQuick } from "entities/token";

export const ToggleVariantPayment = () => {
  // const isBuy = useUnit($isBuyQuick)
  return (
    <></>
    // <Checkbox
    //   checkedIcon={{ name: 'usdt', className: 'text-darkGray-3' }}
    //   uncheckedIcon={{ name: isBuy ? 'solana' : 'solana', className: 'text-darkGray-3' }}
    //   variant="switch"
    //   $isChecked={$isChecked}
    //   toggled={toggled}
    // />
  )
}