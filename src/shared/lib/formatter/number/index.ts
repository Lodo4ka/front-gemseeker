import { round } from './round';
import { formatSmallNumber } from './small-number';
import { uiDefault, uiDefaultWithDollar } from './ui-default';
import { roundToNearest } from './round-nearest';
export const number = {
  round,
  uiDefault,
  formatSmallNumber,
  roundToNearest,
  uiDefaultWithDollar,
};
