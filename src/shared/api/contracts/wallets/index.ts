import { all, wallet } from './all';
import { create } from './create';
import { balances } from './balances';
import { tx } from './tx';
import { txHistory } from './tx-history';
import { deposit } from './deposit';
import { balancesTokens } from './balances-tokens';

export const wallets = {
  all,
  wallet,
  create,
  balances,
  tx,
  txHistory,
  deposit,
  balancesTokens,
};
